#!/bin/sh
set -o errexit

HISTORY="/home/user/.config/dmenu/dmenu.list"
INPUT=$(dmenu "$@" < "$HISTORY")

# aliases
case "$INPUT" in
	telegram)
		CMD="telegram-desktop" ;;
	1c)
		CMD="1cestart" ;;
	*)
		CMD=$INPUT ;;
esac

exec "$CMD"
